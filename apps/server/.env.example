# Database (SQLite for local dev, PostgreSQL for team)
DATABASE_URL="file:./data/api-client.db"
# DATABASE_URL="postgresql://user:password@localhost:5432/api-client"

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=http://localhost:5173

# JWT secret for signing tokens (OAuth2 state, etc.)
JWT_SECRET=change-me-in-production
